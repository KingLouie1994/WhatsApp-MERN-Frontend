// Imports for styling
import styled from "styled-components";

const SidebarChat = () => {
  return (
    <div>
      <h1>Chat</h1>
    </div>
  );
};

// Styled Components
const StyledSidebarChat = styled.div``;

export default SidebarChat;
