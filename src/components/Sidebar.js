// Imports for styling
import styled from "styled-components";

const Sidebar = () => {
  return (
    <StyledSidebar>
      <h1>Sidebar</h1>
    </StyledSidebar>
  );
};

// Styled Components
const StyledSidebar = styled.div`

`;

export default Sidebar;
